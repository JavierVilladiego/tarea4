"use strict";
let calculParImpar = (num) => {
    if (num % 2 == 0) {
        return document.write(`El numero  ${num}  es PAR`);
    }
    else {
        return document.write(`El numero ${num} es IMPAR`);
    }
};
calculParImpar(5);
//# sourceMappingURL=app.js.map